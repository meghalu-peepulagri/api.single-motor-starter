
// Cases get in future in this process

Assign User to device 
1. in mobile we already when assigning device to user motor name asking, so user can motor name can see.
2. Assign device to user. here also a we want ask a motor name. if directly assign device to user, he cant add again a motor name or 
    we add with a default motor name when adding device at web level assign.